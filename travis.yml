language: php
php:
  - 5.4
  - 5.5
  - 5.6
  - hhvm
  - nightly

before_script:
  - travis_retry composer self-update

script:
  - phpunit --coverage-text --coverage-clover=coverage.clover

branches:
  only:
    - master
    - develop

